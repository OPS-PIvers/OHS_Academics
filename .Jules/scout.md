## 2024-02-14 - Duplicate Function Definitions in GAS **Risk:** Defining `doGet` multiple times in a single .js file (or across files in GAS) causes the last definition to overwrite previous ones, silently breaking existing functionality. **Prevention:** Always check for existing entry points (doGet, doPost, onOpen) and refactor them instead of appending new definitions. Use a single router function.