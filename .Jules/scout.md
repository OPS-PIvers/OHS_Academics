## 2024-05-22 - Inconsistent Name Normalization **Risk:** `sendCaseManagerSummaryEmails` uses custom logic (`cleanFirst + cleanLast`) to match student names, while `generateNameKey` uses `lastname|firstname`. This inconsistency could lead to missed matches if data formats change. **Prevention:** We added a unit test for `generateNameKey` to verify its robustness. Future refactors should standardize on `generateNameKey`.
